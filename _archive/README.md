# Archived pages

Previous JetWages marketing / privacy HTML kept for reference.

These files are **not meant to be public**. On GitHub Pages, Jekyll skips directories whose names start with `_`, so this folder is excluded from the published site. Do not add a root `.nojekyll` file.

Open `index.html` locally from this folder if you need to preview the old design; asset paths point at the live site root (`../`).
